package com.chiton.api.repository;

import com.chiton.api.entity.Production_order;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Production_OrderRepository extends JpaRepository<Production_order,Long> {
}
